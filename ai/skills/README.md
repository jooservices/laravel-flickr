# AI skills (package)

Canonical agent rules live in [`AGENTS.md`](../../AGENTS.md).

When changing this package:

1. Keep sync/page-level API only
2. Do not add queues or crawl engine
3. Run `composer check` before claiming done
